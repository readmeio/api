const ProductionRunUnit = {
  "type": "string",
  "description": "Production run unit\n\n`BOX` `SET`",
  "enum": [
    "BOX",
    "SET"
  ],
  "title": "ProductionRunUnit",
  "x-readme-ref-name": "ProductionRunUnit"
} as const;
export default ProductionRunUnit
